/**
 * =====================================================
 * 文件名: 01_func_ptr_basic.c
 * 所属章节: 第九章
 * 对应小节: 一-1 函数名到底是什么？
 * 演示内容: 将函数名赋值给函数指针，通过函数指针调用函数
 * =====================================================
 *
 * 核心知识点：
 *   - 函数名本身就代表函数在代码区中的入口地址
 *   - 函数名在很多表达式中会自动转换为函数指针
 *   - 通过函数指针可以像调用原函数一样调用
 *
 * 编译：gcc -Wall -o 01_func_ptr_basic 01_func_ptr_basic.c
 * 运行：./01_func_ptr_basic
 */

#include <stdio.h>

/* 一个简单的加法函数 */
int add(int a, int b)
{
    return a + b;
}

int main(void)
{
    /* 声明一个函数指针 p：
     *   int (*p)(int, int)
     *   ├── int          返回值类型
     *   ├── (*p)          括号必须加！表示 p 是指针
     *   └── (int, int)    参数类型列表
     */
    int (*p)(int, int) = add;
    /* 等价写法：int (*p)(int, int) = &add;
     * add 作为函数名，在表达式中自动转换为该函数的入口地址 */

    /* 通过函数指针 p 调用 add 函数
     *   p(3, 5)  等价于 add(3, 5)
     *   也可以写成 (*p)(3, 5)，含义相同
     */
    printf("可以把 add 保存到函数指针 p 中，并通过 p 调用：%d\n", p(3, 5));

    return 0;
}

/* ================================================================
 * 预期输出：
 *
 *   可以把 add 保存到函数指针 p 中，并通过 p 调用：8
 *
 * 总结：
 *   ① 函数名 add 本身即函数入口地址，可赋值给函数指针。
 *   ② 声明函数指针时必须用 int (*p)(int, int)，括号不可省略。
 *   ③ 调用时 p(实参) 等价于原函数名调用。
 * ================================================================ */
