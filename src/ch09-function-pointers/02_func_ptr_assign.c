/**
 * =====================================================
 * 文件名: 02_func_ptr_assign.c
 * 所属章节: 第九章
 * 对应小节: 一-3 给函数指针赋值并调用
 * 演示内容: 同一个函数指针先后指向不同函数，实现灵活的函数切换
 * =====================================================
 *
 * 核心知识点：
 *   - 函数指针变量可以重新赋值，指向签名匹配的不同函数
 *   - 签名 = 返回值类型 + 参数类型列表（函数名不参与）
 *   - 调用方式：pFunc(参数) 或 (*pFunc)(参数)
 *
 * 编译：gcc -Wall -o 02_func_ptr_assign 02_func_ptr_assign.c
 * 运行：./02_func_ptr_assign
 */

#include <stdio.h>

/* 加法函数：签名 int (int, int) */
int add(int a, int b)
{
    return a + b;
}

/* 减法函数：签名 int (int, int) — 与 add 签名完全一致 */
int subtract(int a, int b)
{
    return a - b;
}

int main(void)
{
    /* 声明函数指针 pFunc，它能指向任何"返回 int、接受两个 int"的函数 */
    int (*pFunc)(int, int);

    /* ---- 第一步：指向 add，调用加法 ---- */
    pFunc = add;                           /* 用函数名赋值，不需要 &       */
    int result = pFunc(10, 5);             /* 等价于 add(10, 5)            */
    printf("10 + 5 = %d\n", result);

    /* ---- 第二步：改为指向 subtract，调用减法 ---- */
    pFunc = subtract;                      /* pFunc 改存 subtract 的地址  */
    result = pFunc(10, 5);                 /* 等价于 subtract(10, 5)       */
    printf("10 - 5 = %d\n", result);

    /* pFunc 本身只是一个"存函数地址的变量"，赋值不同的函数地址后，
     * 它就调用不同的函数——这就是函数指针带来的灵活性。 */

    return 0;
}

/* ================================================================
 * 预期输出：
 *
 *   10 + 5 = 15
 *   10 - 5 = 5
 *
 * 总结：
 *   ① 同一个函数指针可以在不同时刻指向不同的函数。
 *   ② 赋值时直接用函数名即可（pFunc = funcName）。
 *   ③ 只有签名匹配（返回值类型 + 参数类型列表相同）才能赋值。
 * ================================================================ */
