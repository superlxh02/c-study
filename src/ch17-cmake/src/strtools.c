/**
 * @file    strtools.c
 * @chapter 第十七章 - C语言统一项目构建系统——CMake初识
 * @section 三、进阶与总结 → 3. 动手练习
 * @brief   字符串工具模块的源文件
 *
 * 实现 my_strlen() 函数：遍历字符数组直到遇到 '\0'（空字符），返回计数。
 */

#include "strtools.h"          /* 包含自己的头文件，确保声明与定义一致 */

/* -------------------------------------------------------
 *  计算字符串长度
 *  str: 指向字符数组首地址的指针
 *  返回: 字符串中字符的个数（不包含结尾的 '\0'）
 *
 *  算法：用 while 循环逐字符检查，直到 '\0'，
 *        每检查一个非 '\0' 字符，计数器 len 加 1
 * -------------------------------------------------------*/
int my_strlen(const char *str)
{
    int len = 0;                                    /* 计数器：字符个数 */
    while (str[len] != '\0')                        /* 只要当前字符不是结尾符 */
    {
        len++;                                      /* 计数 +1，继续检查下一个 */
    }
    return len;                                     /* 返回字符串长度 */
}
