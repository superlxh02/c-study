/**
 * =====================================================
 * 文件名: 12_while_loop.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 六、2. while 循环（当型循环）
 * 演示内容: while 循环的基本用法 —— 当条件成立时重复执行循环体
 *           先判断条件，条件成立才执行循环体
 *           循环体内部必须有让条件最终变为假的代码，否则会死循环
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* count 是循环计数器，从 1 开始 */
    int count = 1;

    /* while 循环的基本结构：
     *   while (条件)
     *   {
     *       循环体语句;
     *   }
     *
     * 执行流程：
     *   1. 判断 count <= 5 是否成立
     *   2. 成立 → 执行 { } 中的代码，然后回到第1步
     *   3. 不成立 → 跳过循环，执行后面的代码 */
    while (count <= 5)
    {
        /* 输出当前是第几次循环 */
        printf("第 %d 次循环\n", count);

        /* count = count + 1 是关键语句：
         * 每次循环让 count 增加 1，最终让条件 count <= 5 变为假
         * 如果没有这行，count 始终是 1，条件永远为真 → 死循环！ */
        count = count + 1;
    }

    /* 当 count 变成 6 时，条件 6 <= 5 为假，循环结束，执行此句 */
    printf("循环结束了！\n");

    return 0;
}

/* ============================================================
 * 执行过程分析表：
 *   轮次  | count 的值 | count <= 5? | 执行的操作
 *   第1次 |      1     |     真       | 输出"第1次"，count 变成 2
 *   第2次 |      2     |     真       | 输出"第2次"，count 变成 3
 *   第3次 |      3     |     真       | 输出"第3次"，count 变成 4
 *   第4次 |      4     |     真       | 输出"第4次"，count 变成 5
 *   第5次 |      5     |     真       | 输出"第5次"，count 变成 6
 *   第6次 |      6     |     假       | 条件不满足，退出循环
 *
 * 编译: clang while_demo.c -o while_demo.exe
 * 运行: while_demo.exe
 *
 * 预期输出:
 *   第 1 次循环
 *   第 2 次循环
 *   第 3 次循环
 *   第 4 次循环
 *   第 5 次循环
 *   循环结束了！
 *
 * 重要提醒：循环体内必须有能让条件最终变为假的代码，
 * 否则会导致"死循环"（程序永远停在循环中，不会结束）。
 * ============================================================
 */
