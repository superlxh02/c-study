/**
 * =====================================================
 * 文件名: 08_if_else_demo.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 五、3. if-else 语句（如果...否则...）
 * 演示内容: if-else 双分支判断 —— 条件成立执行 if 分支，
 *           条件不成立执行 else 分支，两条路必走其一
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* score = 55，不及格 */
    int score = 55;

    /* if-else 语句的基本结构：
     *   if (条件)
     *   {
     *       条件成立时执行的语句;
     *   }
     *   else
     *   {
     *       条件不成立时执行的语句;
     *   }
     *
     * 执行流程：
     *   1. 判断 score >= 60 → 55 >= 60 为假
     *   2. 条件不成立，跳到 else 分支
     *   3. 执行 printf("不及格，需要补考。\n")
     *
     * 如果把 score 改成 75，条件成立，执行 if 分支输出"及格了！"
     * 无论条件是否成立，两条分支总会执行一条 */
    if (score >= 60)
    {
        printf("及格了！\n");
    }
    else
    {
        printf("不及格，需要补考。\n");
    }

    return 0;
}

/* ============================================================
 * 执行流程图：
 *         开始
 *          |
 *     score >= 60 ?
 *     /         \
 *    是          否
 *    |           |
 *  输出"及格"  输出"不及格"
 *    |           |
 *         结束
 *
 * 编译: clang if_else_demo.c -o if_else_demo.exe
 * 运行: if_else_demo.exe
 *
 * 预期输出（score = 55）:
 *   不及格，需要补考。
 *
 * 规则:
 *   else 不能单独使用，必须与 if 配对
 *   else 后面不需要写条件，它自动对应"前面的条件都不成立"
 * ============================================================
 */
