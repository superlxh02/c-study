/**
 * =====================================================
 * 文件名: 10_nested_if.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 五、5. if 语句的嵌套
 * 演示内容: 在一个 if 语句内部再嵌套另一个 if 语句
 *           实现更复杂的多层条件判断逻辑
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* score = 75，先判断是否及格，再判断是否优秀 */
    int score = 75;

    /* 外层 if：先判断是否及格（score >= 60）
     * 75 >= 60 为真，进入外层 if 的花括号 */
    if (score >= 60)
    {
        /* 及格了，输出提示信息 */
        printf("及格了\n");

        /* 内层 if：在及格的基础上，进一步判断是否达到优秀
         * 75 >= 90 为假，所以内层的 printf 不会执行 */
        if (score >= 90)
        {
            printf("而且非常优秀！\n");
        }
        /* 注意：如果外层条件不成立，内层 if 根本不会被检查
         * 因为代码根本不会进入外层 if 的 { } 中 */
    }
    else
    {
        /* 外层条件不成立（score < 60）时执行 */
        printf("不及格\n");
    }

    return 0;
}

/* ============================================================
 * 嵌套 if 的执行逻辑（以 score = 75 为例）：
 *   1. 外层判断 score >= 60 → 75 >= 60 为真
 *   2. 执行外层的 printf("及格了\n")
 *   3. 内层判断 score >= 90 → 75 >= 90 为假
 *   4. 跳过内层的 printf("而且非常优秀！\n")
 *   5. 外层 if 结束，跳过 else 分支
 *
 * 如果 score = 95：
 *   输出"及格了"和"而且非常优秀！"
 *
 * 如果 score = 50：
 *   外层条件不成立，直接跳到 else，输出"不及格"
 *
 * 编译: clang nested_if.c -o nested_if.exe
 * 运行: nested_if.exe
 *
 * 预期输出（score = 75）:
 *   及格了
 *
 * 嵌套 if 注意事项：
 *   - 内层 if 只有在外层条件成立时才会被执行
 *   - 注意缩进，让代码层次清晰，便于阅读
 *   - 嵌套层数不宜过深（一般不超过3层），否则代码难以理解
 * ============================================================
 */
