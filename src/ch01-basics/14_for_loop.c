/**
 * =====================================================
 * 文件名: 14_for_loop.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 六、4. for 循环（计数型循环）
 * 演示内容: for 循环的基本用法 —— 计数器初始化、条件判断、
 *           计数器更新三者集中写在一起，清晰明了
 *           特别适合已知循环次数的场景
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* for 循环的基本结构：
     *   for (初始化; 条件; 更新)
     *   {
     *       循环体语句;
     *   }
     *
     * 三个部分的含义：
     *   初始化：int i = 1  —— 循环开始前执行一次，设置计数器
     *   条件：   i <= 5    —— 每次循环前判断，为真则继续
     *   更新：   i = i + 1 —— 每次循环结束后执行，更新计数器
     *
     * 注意：i 是在 for 的初始化部分声明的，它的作用域仅限于 for 循环 */
    for (int i = 1; i <= 5; i = i + 1)
    {
        printf("第 %d 次循环\n", i);
    }

    printf("循环结束了！\n");

    return 0;
}

/* ============================================================
 * 执行过程分析：
 *   1. int i = 1   —— 初始化，i 的值设为 1（只执行一次）
 *   2. i <= 5      —— 判断，1 <= 5 为真，进入循环体
 *   3. printf(...) —— 输出"第 1 次循环"
 *   4. i = i + 1   —— 更新，i 变成 2
 *   5. i <= 5      —— 判断，2 <= 5 为真，进入循环体
 *   6. printf(...) —— 输出"第 2 次循环"
 *   7. i = i + 1   —— 更新，i 变成 3
 *   ...以此类推...
 *   当 i 变成 6 时，6 <= 5 为假，循环结束
 *
 * 简写形式：i++ 完全等价于 i = i + 1
 *   for (int i = 1; i <= 5; i++)
 *   {
 *       printf("第 %d 次循环\n", i);
 *   }
 *
 * 编译: clang for_loop.c -o for_loop.exe
 * 运行: for_loop.exe
 *
 * 预期输出:
 *   第 1 次循环
 *   第 2 次循环
 *   第 3 次循环
 *   第 4 次循环
 *   第 5 次循环
 *   循环结束了！
 *
 * 三种循环的选择建议：
 *   - 知道循环次数（如"重复10次"）→ 用 for
 *   - 不知道循环次数，满足条件就继续 → 用 while 或 do-while
 *   - 必须先执行一次再判断 → 用 do-while
 * ============================================================
 */
