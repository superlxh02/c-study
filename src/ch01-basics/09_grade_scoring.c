/**
 * =====================================================
 * 文件名: 09_grade_scoring.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 五、4. if-else if-else 语句（多条件判断）
 * 演示内容: 多分支条件判断 —— 根据分数给出等级
 *           90分及以上→优秀，80分及以上→良好，
 *           60分及以上→及格，60分以下→不及格
 *           关键规则：多个分支中最多只有一个会被执行
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* 考试成绩：85分 */
    int score = 85;

    /* if-else if-else 多分支结构：
     *   从上到下逐个检查条件
     *   一旦某个条件成立，执行对应分支后立即跳出整个结构
     *   后面的条件不会再被检查 */
    if (score >= 90)
    {
        /* 90分及以上 → 优秀 */
        printf("等级：优秀\n");
    }
    else if (score >= 80)
    {
        /* 80分及以上（但不到90分，因为上面的条件已经过滤掉了 >=90 的情况）
         * 85 >= 80 为真，执行这个分支，输出"良好" */
        printf("等级：良好\n");
    }
    else if (score >= 60)
    {
        /* 60分及以上（但不到80分）→ 及格
         * 注意：这个条件只有当 score < 80 时才会被检查 */
        printf("等级：及格\n");
    }
    else
    {
        /* 以上所有条件都不成立（即 score < 60）→ 不及格
         * else 是"兜底"分支，不需要写条件 */
        printf("等级：不及格\n");
    }

    return 0;
}

/* ============================================================
 * 执行过程（以 score = 85 为例）：
 *   1. 判断 score >= 90  → 85 >= 90 为假，继续
 *   2. 判断 score >= 80  → 85 >= 80 为真，输出"良好"，跳出
 *   3. 后续的 else if 和 else 都被跳过
 *
 * 不同分数的输出：
 *   score = 95 → "等级：优秀"
 *   score = 85 → "等级：良好"
 *   score = 70 → "等级：及格"
 *   score = 50 → "等级：不及格"
 *
 * 编译: clang grade.c -o grade.exe
 * 运行: grade.exe
 *
 * 预期输出（score = 85）:
 *   等级：良好
 *
 * 重要规则：if-else if-else 结构中，最多只有一个分支会被执行。
 * 一旦某个条件满足，后面的条件都会被跳过。
 * ============================================================
 */
