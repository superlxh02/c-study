/**
 * =====================================================
 * 文件名: 22_math_header.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 八、4. 动手练习：使用 math.h
 * 演示内容: 使用 math.h 头文件中的数学函数
 *           sqrt(x)  —— 计算平方根
 *           pow(x,y) —— 计算 x 的 y 次方
 *           fabs(x)  —— 计算浮点数的绝对值
 *           注意：某些平台编译时需要加 -lm 链接数学库
 * =====================================================
 */

#include <stdio.h>
#include <math.h>   /* 包含数学函数头文件，提供 sqrt, pow, fabs 等 */

int main(void)
{
    /* ---- sqrt(x)：计算平方根 ----
     * sqrt(16) 计算 16 的平方根 = 4.0
     * sqrt 函数接收一个 double 参数，返回 double 类型的结果
     * 参数必须是非负数（负数会导致数学错误） */
    printf("16 的平方根 = %f\n", sqrt(16));

    /* ---- pow(x, y)：计算 x 的 y 次方 ----
     * pow(2, 3) 计算 2 的 3 次方 = 8.0
     * pow(2.0, 3.0) 也是 8.0
     * 两个参数都可以是整数或小数，返回 double 类型
     * %f 用于输出 double 类型的值 */
    printf("2 的 3 次方 = %f\n", pow(2, 3));

    /* ---- fabs(x)：计算浮点数的绝对值 ----
     * fabs(-3.14) 返回 -3.14 的绝对值 = 3.14
     * fabs 针对浮点数（float/double），int 的绝对值用 abs()（在 stdlib.h 中）
     * 参数可以是正数、负数或零 */
    printf("-3.14 的绝对值 = %f\n", fabs(-3.14));

    return 0;
}

/* ============================================================
 * 编译（在 Linux/macOS 的 GCC 或 Clang 环境中可能需要 -lm）：
 *   clang math_demo.c -o math_demo.exe -lm
 *   math_demo.exe
 *
 * Windows 上的 Clang 工具链通常不需要单独写 -lm；如果命令提示
 * 不认识该选项，就去掉它并按当前平台的 Clang 链接规则编译。
 *
 * 预期输出:
 *   16 的平方根 = 4.000000
 *   2 的 3 次方 = 8.000000
 *   -3.14 的绝对值 = 3.140000
 *
 * math.h 中的其他常用函数：
 *   sin(x), cos(x), tan(x)  —— 三角函数（参数是弧度）
 *   log(x)                   —— 自然对数
 *   log10(x)                 —— 以10为底的对数
 *   ceil(x)                  —— 向上取整
 *   floor(x)                 —— 向下取整
 *   round(x)                 —— 四舍五入（C99起）
 * ============================================================
 */
