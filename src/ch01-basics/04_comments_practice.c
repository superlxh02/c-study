/**
 * =====================================================
 * 文件名: 04_comments_practice.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 二、5. 动手练习（注释）
 * 演示内容: C语言中两种注释方式的用法
 *           单行注释（以 // 开头）和块注释（斜杠星号 ... 星号斜杠）
 *           注释不会被编译，不影响程序运行
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* 单行注释：以 // 开头，从 // 到行尾的所有内容都是注释
     * 下面这行代码在屏幕上输出 Hello */
    printf("Hello\n");

    /* 多行注释：以结束
     * 可以跨越多行，中间的所有内容都是注释
     * 常用于写函数说明、作者信息、修改记录等
     *
     *   作者：我自己
     *   日期：2026年
     *   功能：练习使用注释
     */
    printf("World\n");  /* 单行注释也可以写在代码后面（行尾注释），输出 World */

    return 0;
}

/* ============================================================
 * C语言注释的规则：
 *
 * 1. 单行注释：// 开头，到行尾结束
 * 2. 块注释：以“斜杠星号”开头，以“星号斜杠”结束，可以跨行
 * 3. 多行注释不能嵌套（注释里面不能再放另一个多行注释）
 * 4. 注释不会被编译，对程序运行没有任何影响
 * 5. 注释可以用于暂时禁用某些代码（调试时很有用）
 *
 * 编译: clang comments.c -o comments.exe
 * 运行: comments.exe
 *
 * 预期输出:
 *   Hello
 *   World
 *
 * 尝试练习:
 *   1. 在 printf("Hello\n"); 前面加上 //，把它变成注释，观察输出变化
 *   2. 用多行注释把 printf("World\n"); 注释掉，观察输出变化
 *   3. 在代码中添加你自己的注释
 * ============================================================
 */
