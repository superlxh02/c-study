/**
 * =====================================================
 * 文件名: 06_operators_practice.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 四、3. 动手练习（运算符）
 * 演示内容: C语言算术运算符（+, -, *, /, %）和
 *           比较运算符（==, !=, >, <, >=, <=）
 *           重点演示整数除法截断现象和 %% 的 printf 写法
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* 声明两个整数变量用于运算演示 */
    int a = 10;
    int b = 3;

    /* ---- 算术运算 ----
     * + 加法、- 减法、* 乘法、/ 除法、% 取余（求余数）
     * 注意：C语言中乘号用 * 而不是 ×，除号用 / 而不是 ÷ */
    printf("a + b = %d\n", a + b);    /* 10 + 3 = 13 */
    printf("a - b = %d\n", a - b);    /* 10 - 3 = 7  */
    printf("a * b = %d\n", a * b);    /* 10 * 3 = 30 */
    printf("a / b = %d\n", a / b);    /* 10 / 3 = 3（整数除法，直接丢弃小数，不是四舍五入） */
    printf("a %% b = %d\n", a % b);   /* 10 % 3 = 1（10 除以 3 商 3 余 1） */

    /* 输出一个空行，让输出看起来更清晰 */
    printf("\n");

    /* ---- 比较运算 ----
     * == 等于, != 不等于, > 大于, < 小于, >= 大于等于, <= 小于等于
     * 比较运算的结果在 C 语言中是整数：1 表示"真"，0 表示"假"
     * 特别注意：判断相等用两个等号 ==，一个等号 = 是赋值！ */
    printf("a == b: %d\n", a == b);   /* 10 等于 3？否 → 0（假） */
    printf("a != b: %d\n", a != b);   /* 10 不等于 3？是 → 1（真） */
    printf("a > b:  %d\n", a > b);    /* 10 大于 3？是   → 1（真） */
    printf("a <= b: %d\n", a <= b);   /* 10 小于等于 3？否 → 0（假） */

    return 0;
}

/* ============================================================
 * 编译: clang operators.c -o operators.exe
 * 运行: operators.exe
 *
 * 预期输出:
 *   a + b = 13
 *   a - b = 7
 *   a * b = 30
 *   a / b = 3
 *   a % b = 1
 *
 *   a == b: 0
 *   a != b: 1
 *   a > b:  1
 *   a <= b: 0
 *
 * 关键知识点:
 *   1. printf("a %% b = %d\n", a % b) 中的 %% 不是取余运算，
 *      它是为了在输出字符串中显示一个 % 符号（因为 % 在 printf
 *      中有特殊含义，所以用 %% 来"转义"）
 *   2. 整数 / 整数 = 整数，小数部分直接丢弃（不是四舍五入）
 *      如果想得到小数结果，至少一个操作数写成浮点数：
 *      float c = 10.0 / 3;  // 结果是 3.333...
 *   3. 取余运算符 % 只对整数有效
 * ============================================================
 */
