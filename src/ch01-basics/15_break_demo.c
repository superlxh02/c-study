/**
 * =====================================================
 * 文件名: 15_break_demo.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 六、6.1 break（跳出循环）
 * 演示内容: break 关键字的作用 —— 立即终止整个循环
 *           跳出到循环后面的代码继续执行
 *           常用于在特定条件下提前结束循环
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* for 循环从 1 到 10，但在 i == 5 时用 break 提前跳出 */
    for (int i = 1; i <= 10; i++)
    {
        /* 当 i 等于 5 时，触发 break 条件 */
        if (i == 5)
        {
            /* 输出提示信息，说明遇到了 5 */
            printf("遇到 5，提前结束循环\n");

            /* break 关键字：立即终止当前循环
             * 程序会跳出 for 循环，执行循环后面的 printf */
            break;
        }

        /* 只有当循环没有被 break 中断时，这行才会执行
         * 因此不会输出 i = 5 时的"当前数字：5" */
        printf("当前数字：%d\n", i);
    }

    /* 循环外的代码，无论循环是正常结束还是被 break 中断，
     * 都会继续执行 */
    printf("循环外的代码\n");

    return 0;
}

/* ============================================================
 * 编译: clang break_demo.c -o break_demo.exe
 * 运行: break_demo.exe
 *
 * 预期输出:
 *   当前数字：1
 *   当前数字：2
 *   当前数字：3
 *   当前数字：4
 *   遇到 5，提前结束循环
 *   循环外的代码
 *
 * 关键观察：
 *   - i = 4 时的输出正常
 *   - i = 5 时，if 条件成立，执行 break 前先打印了"遇到5"
 *   - 由于 break 跳出了循环，"当前数字：5"没有被输出
 *   - i = 6 到 10 的循环迭代也被跳过了
 *   - 循环外的代码正常执行
 *
 * 注意：break 只对最内层的循环有效。
 *       如果有多层嵌套循环，break 只跳出它所在的那一层。
 * ============================================================
 */
