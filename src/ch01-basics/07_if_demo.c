/**
 * =====================================================
 * 文件名: 07_if_demo.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 五、2. if 语句（如果）
 * 演示内容: 最基本的 if 条件判断语句
 *           如果条件成立（真），执行花括号中的代码
 *           如果条件不成立（假），跳过花括号中的代码
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* 声明一个整数变量 score，表示考试成绩 */
    int score = 85;

    /* if 语句的基本结构：
     *   if (条件)
     *   {
     *       条件成立时要执行的语句;
     *   }
     *
     * 条件 score >= 60 的意思是 "score 大于或等于 60"
     * 85 >= 60 成立（为真），所以执行 printf 输出"恭喜你，及格了！"
     * 如果把 score 改成 50，条件不成立，printf 就不会执行 */
    if (score >= 60)
    {
        printf("恭喜你，及格了！\n");
    }

    return 0;
}

/* ============================================================
 * 编译: clang if_demo.c -o if_demo.exe
 * 运行: if_demo.exe
 *
 * 预期输出（当 score = 85 时）:
 *   恭喜你，及格了！
 *
 * 如果修改 score = 50 再编译运行，则没有任何输出，
 * 因为 50 >= 60 为假，if 里面的代码被跳过。
 *
 * if 语句规则:
 *   1. if 后面必须跟圆括号 ()，里面写条件表达式
 *   2. 条件为真（非0）时执行 { } 中的代码
 *   3. 条件为假（0）时跳过 { } 中的代码
 *   4. 如果分支只有一条语句，花括号可以省略，但不建议初学者这样做
 * ============================================================
 */
