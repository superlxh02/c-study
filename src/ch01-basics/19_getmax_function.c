/**
 * =====================================================
 * 文件名: 19_getmax_function.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 七、4. 函数的调用
 * 演示内容: 带返回值的函数调用全过程
 *           getMax 函数使用 if-else 逻辑比较两个整数并返回较大值
 *           展示了函数调用的跳转—执行—返回机制
 * =====================================================
 */

#include <stdio.h>

/*
 * 函数定义：getMax
 * 功能：比较两个整数 a 和 b，返回其中较大的值
 *
 * 参数：
 *   int a —— 第一个整数
 *   int b —— 第二个整数
 * 返回值：a 和 b 中的较大值
 */
int getMax(int a, int b)
{
    /* 使用 if-else 比较大小 */
    if (a > b)
    {
        return a;    /* a 比 b 大，返回 a */
    }
    else
    {
        return b;    /* a 不大于 b（即 b >= a），返回 b */
    }
    /* 注意：return 语句不仅返回一个值，还会立即结束函数的执行
     *       函数中 return 之后的代码不会被执行 */
}

int main(void)
{
    /* ---- 函数调用的执行过程 ----
     *
     * 以 getMax(5, 3) 为例：
     *   1. 程序执行到 getMax(5, 3)
     *   2. 跳转到 getMax 函数定义处
     *   3. 参数 a=5, b=3（实参的值复制给形参）
     *   4. 执行函数体：5 > 3 为真，return a 即 return 5
     *   5. 带着返回值 5 跳回调用处
     *   6. max1 被赋值为 5 */

    int max1 = getMax(5, 3);       /* 5 和 3 中的较大值是 5 */
    int max2 = getMax(10, 20);     /* 10 和 20 中的较大值是 20 */
    int max3 = getMax(-1, -5);     /* -1 和 -5 中的较大值是 -1 */

    /* 输出每次比较的结果 */
    printf("5 和 3 中的较大值：%d\n", max1);
    printf("10 和 20 中的较大值：%d\n", max2);
    printf("-1 和 -5 中的较大值：%d\n", max3);

    return 0;
}

/* ============================================================
 * 编译: clang getmax_function.c -o getmax_function.exe
 * 运行: getmax_function.exe
 *
 * 预期输出:
 *   5 和 3 中的较大值：5
 *   10 和 20 中的较大值：20
 *   -1 和 -5 中的较大值：-1
 *
 * 函数的组成部分回顾：
 *   返回值类型  —— 函数返回结果的数据类型（int, float, void 等）
 *   函数名      —— 标识函数的名字，调用时使用
 *   参数列表    —— 函数的输入，可以有多个或没有
 *   函数体      —— { } 中要执行的代码
 *   return     —— 返回结果给调用者，同时结束函数执行
 * ============================================================
 */
