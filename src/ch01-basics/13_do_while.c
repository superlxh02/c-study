/**
 * =====================================================
 * 文件名: 13_do_while.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 六、3. do-while 循环（直到型循环）
 * 演示内容: do-while 循环的基本用法 —— 先执行一次循环体，
 *           再判断条件，条件成立则继续循环
 *           与 while 的核心区别：do-while 保证循环体至少执行一次
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* count 是循环计数器，从 1 开始 */
    int count = 1;

    /* do-while 循环的基本结构：
     *   do
     *   {
     *       循环体语句;
     *   } while (条件);
     *
     * 注意：while (条件) 后面有分号 ; ，不要漏掉！
     *
     * 执行流程：
     *   1. 先执行一次 { } 中的代码（不管条件是否成立）
     *   2. 判断 count <= 5 是否成立
     *   3. 成立 → 回到第1步
     *   4. 不成立 → 结束循环 */
    do
    {
        printf("第 %d 次循环\n", count);

        /* 每次循环让 count 增加 1，使条件最终变为假 */
        count = count + 1;
    } while (count <= 5);  /* 注意这里的分号！ */

    printf("循环结束了！\n");

    return 0;
}

/* ============================================================
 * while 和 do-while 的核心区别：
 *
 *   while：先判断，后执行 → 可能一次都不执行（条件一开始就不成立）
 *   do-while：先执行，后判断 → 至少执行一次（不管条件是否成立）
 *
 * 示例对比（count 初始值为 10，条件都是 count <= 5）：
 *
 *   // while 版本 —— 循环体一次都不会执行
 *   int count = 10;
 *   while (count <= 5) { printf("不会输出\n"); }
 *
 *   // do-while 版本 —— 循环体会执行一次
 *   int count = 10;
 *   do { printf("会输出一次\n"); } while (count <= 5);
 *
 * 编译: clang do_while.c -o do_while.exe
 * 运行: do_while.exe
 *
 * 预期输出（与普通 while 版本相同）:
 *   第 1 次循环
 *   第 2 次循环
 *   第 3 次循环
 *   第 4 次循环
 *   第 5 次循环
 *   循环结束了！
 * ============================================================
 */
