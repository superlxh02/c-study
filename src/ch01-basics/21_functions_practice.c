/**
 * =====================================================
 * 文件名: 21_functions_practice.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 七、6. 动手练习（函数）
 * 演示内容: 综合练习函数声明、函数定义和函数调用
 *           square  —— 计算平方（有参数，有返回值）
 *           isEven  —— 判断是否为偶数（有参数，有返回值）
 *           printStars —— 打印星号（有参数，无返回值）
 *           函数定义在 main 后面，因此在前面做了声明
 * =====================================================
 */

#include <stdio.h>

/*
 * 函数声明（函数原型）
 * 因为函数定义写在 main 后面，所以这里需要提前声明
 * 告诉编译器：这些函数在后面有完整的定义
 */
int square(int x);               /* 计算 x 的平方，返回整数 */
int isEven(int n);               /* 判断 n 是否为偶数，1=是 0=否 */
void printStars(int count);      /* 打印 count 个星号，无返回值 */

int main(void)
{
    /* ---- 调用 square 函数 ----
     * square(4) 计算 4*4 = 16
     * square(7) 计算 7*7 = 49
     * 函数调用可以直接作为 printf 的参数 */
    printf("4 的平方 = %d\n", square(4));
    printf("7 的平方 = %d\n", square(7));

    printf("\n");  /* 输出空行 */

    /* ---- 调用 isEven 函数 ----
     * isEven(10) 判断 10 是否为偶数，返回 1（真）
     * isEven(7)  判断 7 是否为偶数，返回 0（假）
     * 将函数返回值直接用于 if 条件判断 */
    if (isEven(10) == 1)
    {
        printf("10 是偶数\n");
    }

    if (isEven(7) == 0)
    {
        printf("7 是奇数\n");
    }

    printf("\n");  /* 输出空行 */

    /* ---- 调用 printStars 函数 ----
     * printStars(5) 在屏幕上打印 5 个星号
     * 这个函数没有返回值（void），调用后直接产生输出效果 */
    printf("打印 5 个星号：");
    printStars(5);

    return 0;
}

/*
 * 函数定义：square
 * 功能：计算整数 x 的平方（x * x）
 * 参数：int x —— 要计算平方的整数
 * 返回值：x 的平方值
 */
int square(int x)
{
    return x * x;                /* x * x 是乘法的数学表达式映射 */
}

/*
 * 函数定义：isEven
 * 功能：判断一个整数是否为偶数
 * 参数：int n —— 要判断的整数
 * 返回值：1 表示是偶数，0 表示是奇数
 *
 * 判断原理：如果 n 除以 2 的余数为 0，则是偶数
 *           n % 2 计算 n 除以 2 的余数
 *           == 0 判断余数是否为零
 */
int isEven(int n)
{
    if (n % 2 == 0)
    {
        return 1;    /* 余数为 0 → 偶数 → 返回 1 */
    }
    else
    {
        return 0;    /* 余数不为 0 → 奇数 → 返回 0 */
    }
}

/*
 * 函数定义：printStars
 * 功能：在屏幕上打印指定数量的星号 *
 * 参数：int count —— 要打印的星号数量
 * 返回值：无（void 表示不返回任何值）
 *
 * void 类型说明：
 *   这个函数只产生"副作用"（输出到屏幕），不需要返回值
 *   函数体内可以没有 return 语句，或者写 return;（不带值）
 */
void printStars(int count)
{
    /* 使用 for 循环打印 count 个星号
     * i 从 0 到 count-1，循环 count 次 */
    for (int i = 0; i < count; i++)
    {
        printf("*");             /* 打印一个星号，不换行 */
    }
    printf("\n");                /* 所有星号打印完后换行 */
}

/* ============================================================
 * 编译: clang functions_practice.c -o functions_practice.exe
 * 运行: functions_practice.exe
 *
 * 预期输出:
 *   4 的平方 = 16
 *   7 的平方 = 49
 *
 *   10 是偶数
 *   7 是奇数
 *
 *   打印 5 个星号：*****
 *
 * 尝试练习：
 *   1. 编写一个 getMin 函数，返回两个整数中的较小值
 *   2. 编写一个 factorial 函数，计算 n! = 1*2*3*...*n
 *   3. 编写一个 isPrime 函数，判断一个数是否为素数
 * ============================================================
 */
