/**
 * =====================================================
 * 文件名: 05_variables_practice.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 三、5. 动手练习（变量）
 * 演示内容: C语言基本数据类型的声明、初始化和输出
 *           int（整数）、float（单精度浮点数）、
 *           double（双精度浮点数）、char（字符）
 *           printf 格式占位符：%d, %f, %c
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* ---- 声明整数变量 ----
     * int 类型用于存储整数（正负整数和零），通常占 4 字节
     * 声明格式：类型 变量名 = 初始值; */
    int age = 18;       /* 年龄：18岁 */
    int year = 2026;    /* 年份：2026年 */

    /* ---- 声明小数变量 ----
     * float  用于存储普通精度的小数（约6-7位有效数字），通常占 4 字节
     * double 用于存储高精度的小数（约15-16位有效数字），通常占 8 字节 */
    float height = 1.75;          /* 身高：1.75米（普通精度即可） */
    double pi = 3.1415926;         /* 圆周率：需要较多小数位，用 double */

    /* ---- 声明字符变量 ----
     * char 类型存储单个字符，占 1 字节
     * 字符必须用单引号包裹，如 'A'、'#'（注意不是双引号） */
    char grade = 'A';              /* 成绩等级：A */

    /* ---- 使用 printf 输出变量的值 ----
     * printf 通过"格式占位符"来指定在哪里插入变量的值：
     *   %d  —— 输出有符号十进制整数（对应 int）
     *   %f  —— 输出浮点数（float 会提升为 double，double 也用 %f）
     *   %c  —— 输出单个字符（对应 char）
     * 占位符和后面的参数按顺序一一对应 */
    printf("年龄：%d\n", age);
    printf("年份：%d\n", year);
    printf("身高：%f 米\n", height);
    printf("圆周率：%f\n", pi);
    printf("成绩等级：%c\n", grade);

    return 0;
}

/* ============================================================
 * 编译: clang variables.c -o variables.exe
 * 运行: variables.exe
 *
 * 预期输出:
 *   年龄：18
 *   年份：2026
 *   身高：1.750000 米
 *   圆周率：3.141593
 *   成绩等级：A
 *
 * 注意:
 *   - float 默认输出6位小数，实际值的二进制表示可能与原值略有偏差
 *   - 类型选择建议：年龄/数量/序号用 int，普通小数用 float，
 *     高精度科学计算用 double，单个字符用 char
 *
 * 变量命名规则:
 *   1. 只能使用英文字母、数字和下划线
 *   2. 不能以数字开头（如 2nd 是非法的）
 *   3. 不能是 C 语言保留字（如 int, return, if）
 *   4. 区分大小写（Age 和 age 是不同的变量）
 * ============================================================
 */
