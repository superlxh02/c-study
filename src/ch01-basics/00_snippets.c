/**
 * =====================================================
 * 文件名: 00_snippets.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 各节中的代码片段（非完整可运行程序）
 * 演示内容: 收集文档中的重要代码片段，每个片段用注释说明
 *
 * 注意: 此文件不能直接编译运行，仅供学习参考
 * =====================================================
 */

#include <stdio.h>

// ============================================================
// 片段1: 整数除法 vs 浮点数除法（来自 四、1.1 重要注意：整数除法）
// 演示 整数/整数 会丢弃小数部分；分子或分母有浮点数则结果也是浮点数
// ============================================================
#if 0
int a = 7 / 3;      // a 的值是 2，不是 2.333...（小数部分被丢弃，不是四舍五入）
int b = 5 / 2;      // b 的值是 2，不是 2.5
float c = 7.0 / 3;  // c 的值约等于 2.333（至少有一个操作数是浮点数，结果才是浮点数）
float d = 5.0 / 2;  // d 的值是 2.5
#endif

// ============================================================
// 片段2: while 和 do-while 的区别（来自 六、3.4）
// 演示 while 可能一次都不执行，do-while 至少执行一次
// ============================================================
#if 0
// while 版本：条件一开始就不成立，循环体一次都不会执行
int count = 10;
while (count <= 5)
{
    printf("这句话不会输出\n");
}

// do-while 版本：先执行一次循环体，再判断条件，循环体至少执行一次
count = 10;
do
{
    printf("这句话会输出一次\n");
} while (count <= 5);
#endif

// ============================================================
// 片段3: printf 格式化输出进阶技巧（来自 九、1.4）
// 演示控制小数位数、控制输出宽度、混合输出多个值
// ============================================================
#if 0
// 控制小数位数
float pi = 3.14159;
printf("圆周率（默认）：%f\n", pi);      // 3.141590
printf("圆周率（2位）：%.2f\n", pi);     // 3.14
printf("圆周率（4位）：%.4f\n", pi);     // 3.1416（四舍五入）

// 控制输出宽度（右对齐、左对齐）
int num = 42;
printf("[%d]\n", num);       // [42]
printf("[%5d]\n", num);      // [   42]（占5个字符宽度，右对齐）
printf("[%-5d]\n", num);     // [42   ]（占5个字符宽度，左对齐）

// 混合输出多个值
int age = 18;
float height = 1.75;
char grade = 'A';
printf("小明今年 %d 岁，身高 %.2f 米，成绩 %c\n", age, height, grade);
#endif

// ============================================================
// 片段4: 函数的四种常见形式（来自 七、3.3）
// 演示 有参有返回值 / 无参有返回值 / 有参无返回值 / 无参无返回值
// ============================================================
#if 0
// 形式一：有参数，有返回值 —— 求两个整数的和
int add(int a, int b)
{
    int sum = a + b;
    return sum;
}

// 形式二：没有参数，有返回值 —— 返回固定的数字
int getHundred()
{
    return 100;
}

// 形式三：有参数，没有返回值（返回值类型写 void）—— 打印问候语
void greet(char name[])
{
    printf("你好，%s！\n", name);
    // 没有 return 语句，或者写 return;（不带值）
}

// 形式四：没有参数，没有返回值 —— 打印分隔线
void printLine()
{
    printf("--------------------\n");
}
#endif

// ============================================================
// 片段5: #include 的两种写法（来自 八、3）
// 演示 系统头文件用尖括号 < >，自定义头文件用双引号 " "
// ============================================================
#if 0
#include <stdio.h>      // 尖括号：用于系统头文件，编译器在系统预设目录查找
#include "myheader.h"   // 双引号：用于自定义头文件，先在当前目录查找，找不到再去系统目录
#endif

// ============================================================
// 片段6: scanf 使用注意事项（来自 九、2.4）
// 演示 & 符号的必要性、%c 前加空格、scanf 读取字符串
// ============================================================
#if 0
// 1. 必须在变量名前加 &（取地址符）
int x;
scanf("%d", &x);    // 正确：加了 &，告诉 scanf 把值存到 x 的地址
scanf("%d", x);     // 错误：没有 &，会导致程序崩溃（未定义行为）

// 2. %c 读取字符时前面加空格，跳过缓冲区中的回车符
char grade;
scanf(" %c", &grade);  // %c 前的空格会自动跳过空白字符（空格、回车、Tab）

// 3. 读取字符串（字符数组）时不需要 &
char name[20];
scanf("%19s", name);  // 数组名本身就是地址，所以不需要 &；最多读 19 个可见字符
#endif

// ============================================================
// 主函数（此文件仅用于存放片段，实际不运行）
// ============================================================
int main(void)
{
    printf("此文件收集了文档中的代码片段，不能直接编译运行。\n");
    printf("请查看各个 #if 0 块中的代码片段。\n");
    return 0;
}
