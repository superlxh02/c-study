/**
 * =====================================================
 * 文件名: 25_simple_calculator.c
 * 所属章节: 第一章 - C语言核心基础语法
 * 对应小节: 九、3. 动手练习：简易计算器
 * 演示内容: 综合使用 scanf 输入和 printf 输出
 *           实现加、减、乘、除、取余五种基本运算
 *           包含除数为 0 的安全检查
 *           演示整数除法的截断特性
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /* 声明两个整数变量，用于存储用户输入 */
    int num1, num2;

    /* 程序标题 */
    printf("=== 简易加法计算器 ===\n\n");

    /* 获取用户输入的两个整数
     * 每个 scanf 都会暂停程序，等待用户输入 */
    printf("请输入第一个整数：");
    if (scanf("%d", &num1) != 1)       /* 读取并验证第一个操作数 */
    {
        fprintf(stderr, "第一个输入不是有效整数。\n");
        return 1;
    }

    printf("请输入第二个整数：");
    if (scanf("%d", &num2) != 1)       /* 读取并验证第二个操作数 */
    {
        fprintf(stderr, "第二个输入不是有效整数。\n");
        return 1;
    }

    /* 先提升为 long long 再运算，避免两个 int 的加减乘发生有符号溢出。 */
    long long left = num1;
    long long right = num2;

    /* ---- 计算并输出结果 ----
     * 直接在 printf 的参数中写表达式进行运算（常见写法）
     * printf 会先计算出表达式的值，再用 %d 输出 */
    printf("\n%d + %d = %lld\n", num1, num2, left + right); /* 加法 */
    printf("%d - %d = %lld\n", num1, num2, left - right);   /* 减法 */
    printf("%d * %d = %lld\n", num1, num2, left * right);   /* 乘法 */

    /* ---- 除法需要安全检查 ----
     * 除数不能为 0！数学上除以 0 无意义，程序中会导致崩溃
     * 使用 if 判断 num2 是否为零 */
    if (num2 != 0)
    {
        /* 除数不为 0，正常执行除法和取余
         * 注意：整数除法会丢弃小数部分（不是四舍五入）
         * 例如 10 / 3 = 3（而不是 3.333...） */
        printf("%d / %d = %lld\n", num1, num2, left / right); /* 除法 */
        printf("%d %% %d = %lld\n", num1, num2, left % right); /* 取余 */
    }
    else
    {
        /* 除数为 0，输出错误提示 */
        printf("除数不能为 0！\n");
    }

    return 0;
}

/* ============================================================
 * 编译: clang calculator.c -o calculator.exe
 * 运行: calculator.exe
 *
 * 运行示例1（正常运算）：
 *   === 简易加法计算器 ===
 *
 *   请输入第一个整数：10
 *   请输入第二个整数：3
 *
 *   10 + 3 = 13
 *   10 - 3 = 7
 *   10 * 3 = 30
 *   10 / 3 = 3
 *   10 % 3 = 1
 *
 * 运行示例2（除数为0）：
 *   === 简易加法计算器 ===
 *
 *   请输入第一个整数：10
 *   请输入第二个整数：0
 *
 *   10 + 0 = 10
 *   10 - 0 = 10
 *   10 * 0 = 0
 *   除数不能为 0！
 *
 * 尝试练习：
 *   1. 修改程序，使用 float 类型进行小数运算（%f 读取和输出）
 *   2. 编写程序，输入圆的半径，计算面积（面积 = pi * r^2，pi 取 3.14159）
 *   3. 编写程序，输入三个整数，输出其中的最大值
 * ============================================================
 */
