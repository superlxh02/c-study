/**
 * =====================================================
 * 文件名: 01_stack_variables.c
 * 所属章节: 第六章
 * 对应小节: 二-1. 栈区（Stack）——自动管理的"便签纸"
 * 演示内容: 自动存储期局部变量的生命周期；
 *          并用常见调用栈实现帮助建立直觉
 * =====================================================
 */

#include <stdio.h>

/*
 * func 函数：演示自动局部变量的生命周期
 *
 * 当一个函数被调用时：
 *   1. a、b、c 的生命周期在执行进入相应块时开始
 *   2. 常见未优化实现会在当前栈帧中为它们保留空间
 *   3. 优化器也可以使用寄存器，甚至消除不必要的对象存储
 *   4. 函数返回时，这些自动对象的生命周期结束
 *
 * 这个过程完全由编译器自动管理，程序员无需任何额外操作。
 * 栈的管理方式是 LIFO（后进先出，Last In First Out）。
 */
void func(void)
{
    int a = 10;       // 通常在栈上 —— 整型局部变量
    double b = 3.14;  // 通常在栈上 —— 双精度浮点局部变量
    char c = 'X';     // 通常在栈上 —— 字符局部变量

    printf("func 函数内部：\n");
    printf("  a = %d\n", a);
    printf("  b = %.2f\n", b);
    printf("  c = %c\n", c);
    printf("  它们是自动局部变量（常见实现通常使用栈帧）\n\n");

}  // <-- 函数结束时，a、b、c 自动销毁，它们的内存被回收

int main(void)
{
    printf("=== 栈区局部变量演示 ===\n\n");
    printf("调用 func 之前...\n");

    func();  // 进入 func，栈上分配 a、b、c；返回后，a、b、c 自动销毁

    printf("func 已返回，a、b、c 已销毁，无法再访问\n");

    return 0;
}

/*
 * =====================================================
 * 栈区的特点总结：
 *
 *   管理方式   — 编译器自动分配、自动释放（程序员零负担）
 *   实现方式   — 常见实现使用调用栈，也可能使用寄存器或优化消除
 *   空间大小   — 调用栈容量有限，具体上限由平台和线程配置决定
 *   生命周期   — 自动对象通常随对应代码块的执行开始和结束
 *   溢出风险   — 在函数内定义超大局部数组可能导致栈溢出
 *   比喻       — 便签纸：记录当前正在做的事，做完就扔掉
 *
 * 对比理解：
 *   - 栈区 = 便签纸，用完即弃
 *   - 静态存储区 = 铭牌，一直存在
 *   - 堆区 = 储物仓库，手动租用和归还
 *
 * =====================================================
 * 预期输出:
 * =====================================================
 * === 栈区局部变量演示 ===
 *
 * 调用 func 之前...
 * func 函数内部：
 *   a = 10
 *   b = 3.14
 *   c = X
 *   它们是自动局部变量（常见实现通常使用栈帧）
 *
 * func 已返回，a、b、c 已销毁，无法再访问
 * =====================================================
 */
