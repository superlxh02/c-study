/**
 * =====================================================
 * 文件名: 04_print_addresses.c
 * 所属章节: 第六章
 * 对应小节: 三-1. 变量与地址——内存的门牌号
 * 演示内容: 使用取地址运算符 & 获取变量的内存地址；
 *          使用 %p 格式占位符打印地址值
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    /*
     * 三个不同类型的自动局部变量；由于下面会取地址，
     * 编译器必须为每个对象提供可寻址且互不重叠的存储
     */
    int a = 100;        // 整型变量
    double b = 3.14;    // 双精度浮点变量
    char c = 'Z';       // 字符变量

    /*
     * 使用取地址运算符 & 获取变量的内存地址
     *
     * 语法：&变量名
     * 返回：变量在内存中的起始地址（门牌号）
     *
     * 输出格式：
     *   %p  — printf 中专用于输出指针（地址）的格式占位符
     *   (void *) — 类型转换，确保地址以标准格式输出
     *
     * 地址值通常以十六进制显示（如 000000BFCB5FFBBC）
     */
    printf("变量 a 的值：%d，地址：%p\n", a, (void *)&a);
    printf("变量 b 的值：%.2f，地址：%p\n", b, (void *)&b);
    printf("变量 c 的值：%c，地址：%p\n", c, (void *)&c);

    return 0;
}

/*
 * =====================================================
 * 关键知识点：
 *
 * 1. 地址是什么？
 *    - 每个变量在内存中占据若干个字节，起始位置有一个编号
 *    - 这个编号就是"地址"，通常用十六进制表示
 *    - 可以理解为"第几号内存格子"
 *
 * 2. & 运算符
 *    - &a 读作"取 a 的地址"
 *    - 返回的是 a 在内存中的起始位置
 *
 * 3. %p 格式占位符
 *    - printf 中专用于输出指针（地址）值
 *    - 通常以十六进制格式输出
 *
 * 4. (void *) 类型转换
 *    - 因为 %p 要求 void * 类型的参数
 *    - 这个转换确保了跨平台的兼容性
 *
 * 5. 不要依赖具体地址值
 *    - 具有虚拟内存和 ASLR 的系统可能在每次运行时改变映射位置
 *    - 裸机或其他平台的行为可能不同
 *    - 可移植程序只使用指针关系，不硬编码示例地址
 *
 * =====================================================
 * 预期输出示例（地址值每次运行可能不同）:
 * =====================================================
 * 变量 a 的值：100，地址：000000BFCB5FFBBC
 * 变量 b 的值：3.14，地址：000000BFCB5FFBC0
 * 变量 c 的值：Z，地址：000000BFCB5FFBC4
 * =====================================================
 */
