/**
 * =====================================================
 * 文件名: 08_structs_practice.c
 * 所属章节: 第五章
 * 对应小节: 三-2. 动手练习（结构体综合）
 * 演示内容: 结构体综合练习：
 *           1. 结构体数组存储图书信息并找出最贵的书
 *           2. 结构体嵌套——学生与地址信息
 * =====================================================
 */

#include <stdio.h>
#include <string.h>

// ---- 练习1用到的结构体：图书 ----

// 定义图书结构体，包含书的各项信息
struct Book
{
    char title[50];    // 书名
    char author[30];   // 作者
    int pages;         // 页数
    float price;       // 价格
};

int main(void)
{
    printf("==================== 练习1：图书信息管理 ====================\n\n");

    /*
     * 结构体数组：存储 3 本书的信息
     * 每本书是一个结构体变量，3 本书组成一个数组
     */
    struct Book books[3] = {
        {"C语言入门", "张三", 320, 45.5},
        {"数据结构", "李四", 450, 59.0},
        {"算法导论", "王五", 600, 79.5}
    };

    // 打印所有图书的详细信息
    printf("===== 图书信息表 =====\n");
    for (int i = 0; i < 3; i++)
    {
        printf("书名：%s\n", books[i].title);
        printf("作者：%s\n", books[i].author);
        printf("页数：%d\n", books[i].pages);
        printf("价格：%.1f 元\n", books[i].price);
        printf("----------------------\n");
    }

    /*
     * 找出最贵的书
     * 算法思路：
     *   1. 假设第一本书（下标 0）是最贵的
     *   2. 从第二本开始遍历，如果发现更贵的，更新最大下标
     *   3. 遍历结束后，maxIndex 就是最贵那本书的下标
     */
    int maxIndex = 0;   // 假设第一本最贵
    for (int i = 1; i < 3; i++)
    {
        if (books[i].price > books[maxIndex].price)
        {
            maxIndex = i;    // 发现更贵的，更新记录
        }
    }
    printf("最贵的书是：《%s》，价格 %.1f 元\n\n",
           books[maxIndex].title, books[maxIndex].price);


    printf("==================== 练习2：嵌套结构体——学生与地址 ====================\n\n");

    /*
     * 嵌套结构体示例：一个学生的信息中包含地址，
     * 而地址本身又是一个结构体（包含城市和街道）
     */

    // 地址结构体 —— 被嵌套
    struct Address
    {
        char city[20];     // 城市
        char street[50];   // 街道
    };

    // 人物结构体 —— 外层
    struct Person
    {
        char name[20];              // 姓名
        int age;                    // 年龄
        struct Address addr;        // 地址 —— 嵌套的结构体成员
    };

    // 嵌套初始化：外层 { } 对应 Person，内层 { } 对应 Address
    struct Person person1 = {"小明", 20, {"上海", "南京路100号"}};

    // 使用多级点运算符访问嵌套成员
    printf("姓名：%s\n", person1.name);
    printf("年龄：%d\n", person1.age);
    printf("城市：%s\n", person1.addr.city);     // 两层点运算符
    printf("街道：%s\n", person1.addr.street);   // 两层点运算符

    return 0;
}

/*
 * =====================================================
 * 本章核心知识回顾：
 *
 * 1. 结构体是把不同类型的数据打包在一起的自定义类型
 * 2. 定义用 struct 关键字，花括号后分号不能忘
 * 3. 声明变量时需带 struct 关键字
 * 4. 访问成员用点运算符 .
 * 5. 结构体数组每个元素都是完整结构体
 * 6. 结构体可以嵌套——成员可以是另一个结构体
 * 7. 字符串成员在声明后修改需要用 strcpy，不能直接用 =
 * 8. 结构体可以以传值方式传递给函数或从函数返回
 *
 * =====================================================
 * 预期输出:
 * =====================================================
 * ==================== 练习1：图书信息管理 ====================
 *
 * ===== 图书信息表 =====
 * 书名：C语言入门
 * 作者：张三
 * 页数：320
 * 价格：45.5 元
 * ----------------------
 * 书名：数据结构
 * 作者：李四
 * 页数：450
 * 价格：59.0 元
 * ----------------------
 * 书名：算法导论
 * 作者：王五
 * 页数：600
 * 价格：79.5 元
 * ----------------------
 * 最贵的书是：《算法导论》，价格 79.5 元
 *
 * ==================== 练习2：嵌套结构体——学生与地址 ====================
 *
 * 姓名：小明
 * 年龄：20
 * 城市：上海
 * 街道：南京路100号
 * =====================================================
 */
