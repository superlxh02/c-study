/**
 * =====================================================
 * 文件名: 02_dot_operator.c
 * 所属章节: 第五章
 * 对应小节: 一-4. 访问结构体成员：点运算符 .
 * 演示内容: 使用点运算符 . 读取和修改结构体成员的值；
 *          点运算符是访问结构体成员的"钥匙"
 * =====================================================
 */

#include <stdio.h>

// 定义 Student 结构体类型
struct Student
{
    char name[20];    // 姓名
    int age;          // 年龄
    float score;      // 成绩
};

int main(void)
{
    // 声明并初始化结构体变量
    struct Student stu1 = {"小明", 18, 92.5};

    /*
     * 使用点运算符 . 访问（读取）结构体成员
     * 语法：结构体变量名.成员名
     *
     * 点运算符就像一个"钥匙"，打开结构体这个"包裹"，
     * 取出里面某个具体的东西。
     */
    printf("姓名：%s\n", stu1.name);
    printf("年龄：%d\n", stu1.age);
    printf("成绩：%.1f\n", stu1.score);

    /*
     * 使用点运算符修改成员的值
     * 点运算符不仅可以"读"，也可以"写"——和普通变量赋值一样
     */
    stu1.age = 19;       // 把年龄从 18 改为 19
    stu1.score = 95.0;   // 把成绩从 92.5 改为 95.0

    // 输出修改后的结果
    printf("\n--- 修改后 ---\n");
    printf("年龄：%d\n", stu1.age);
    printf("成绩：%.1f\n", stu1.score);

    return 0;
}

/*
 * =====================================================
 * 预期输出:
 * =====================================================
 * 姓名：小明
 * 年龄：18
 * 成绩：92.5
 *
 * --- 修改后 ---
 * 年龄：19
 * 成绩：95.0
 * =====================================================
 */
