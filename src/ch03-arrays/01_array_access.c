/**
 * =====================================================
 * 文件名: 01_array_access.c
 * 所属章节: 第三章 数组
 * 对应小节: 一、数组基础 → 4. 访问数组元素
 * 演示内容: 通过下标运算符 [ ] 读取和修改数组元素
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    // 声明并完全初始化一个包含 5 个整数的数组
    // 下标范围：0 ~ 4
    int scores[5] = {85, 92, 78, 90, 88};

    // ----- 读取数组元素 -----
    // 数组下标从 0 开始，因此：
    // scores[0] 是第 1 个元素（第一个同学）
    // scores[1] 是第 2 个元素（第二个同学）
    // scores[2] 是第 3 个元素（第三个同学）
    // scores[3] 是第 4 个元素（第四个同学）
    // scores[4] 是第 5 个元素（第五个同学）
    printf("第一个同学的成绩：%d\n", scores[0]);  // 预期输出：85
    printf("第三个同学的成绩：%d\n", scores[2]);  // 预期输出：78
    printf("第五个同学的成绩：%d\n", scores[4]);  // 预期输出：88

    // ----- 修改数组元素 -----
    // 通过下标直接给某个元素赋新值，就像给普通变量赋值一样
    scores[2] = 95;  // 把第 3 个元素（下标为 2）的成绩从 78 改为 95
    printf("修改后第三个同学的成绩：%d\n", scores[2]);  // 预期输出：95

    return 0;
}

/*
 * ============================================================
 * 预期输出：
 * ------------------------------------------------------------
 * 第一个同学的成绩：85
 * 第三个同学的成绩：78
 * 第五个同学的成绩：88
 * 修改后第三个同学的成绩：95
 * ============================================================
 *
 * 要点总结：
 * 1. 下标从 0 开始，第 N 个元素对应的下标是 N-1
 * 2. 读取元素：直接用 数组名[下标]
 * 3. 修改元素：数组名[下标] = 新值;
 * 4. 数组元素可以像普通变量一样参与表达式和赋值
 */
