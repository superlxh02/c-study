/**
 * =====================================================
 * 文件名: 04_array_input.c
 * 所属章节: 第三章 数组
 * 对应小节: 二、数组操作 → 1.3 用循环从键盘输入数组元素
 * 演示内容: 使用 scanf 配合 for 循环批量读入数组元素
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    // 声明一个能存放 5 个整数的数组；成功输入前不得读取其不确定值
    int scores[5];

    // ----- 第一部分：从键盘输入数组元素 -----
    printf("请输入 5 个同学的成绩：\n");
    for (int i = 0; i < 5; i++)
    {
        // i + 1 让提示信息使用人类习惯的编号（第 1 个 ~ 第 5 个）
        printf("第 %d 个：", i + 1);

        // scanf 读取整数，存入 scores[i] 对应的内存地址
        // 注意：&scores[i] 不能省略 & 符号！
        // scores[i] 是一个普通的 int 变量，必须用 & 取地址
        if (scanf("%d", &scores[i]) != 1)
        {
            // 输入失败时立即退出，避免随后读取尚未初始化的数组元素。
            fprintf(stderr, "第 %d 个成绩不是有效整数。\n", i + 1);
            return 1;
        }
    }

    // ----- 第二部分：输出确认输入的内容 -----
    printf("\n你输入的成绩是：\n");
    for (int i = 0; i < 5; i++)
    {
        // %d 后加一个空格，使数字之间有空隙，更易阅读
        printf("%d ", scores[i]);
    }
    printf("\n");  // 最后输出一个换行符，使终端提示符另起一行

    return 0;
}

/*
 * ============================================================
 * 运行示例：
 * ------------------------------------------------------------
 * 请输入 5 个同学的成绩：
 * 第 1 个：85
 * 第 2 个：92
 * 第 3 个：78
 * 第 4 个：90
 * 第 5 个：88
 *
 * 你输入的成绩是：
 * 85 92 78 90 88
 * ============================================================
 *
 * 要点总结：
 * 1. scanf("%d", &scores[i]) —— scores[i] 是 int 变量，前面要加 &
 *    这与 scanf 读普通 int 变量一样（例如 scanf("%d", &n);）
 * 2. 数组元素 scores[i] 等价于一个普通变量，所以取地址的写法不变
 * 3. 可以用同一个循环完成"提示输入+读取"两个步骤
 * 4. 第二个循环确认输入，验证数据是否正确写入
 *
 * 对比：字符串输入时 name 前面不加 &，因为数组名本身代表地址。
 * 但 scores[i] 是数组的一个元素（普通变量），不是整个数组，所以需要 &。
 */
