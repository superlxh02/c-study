/**
 * =====================================================
 * 文件名: 06_find_max_min.c
 * 所属章节: 第三章 数组
 * 对应小节: 二、数组操作 → 3. 经典应用：查找最大值和最小值
 * 演示内容: 通过"先假设、再比较"的算法找出数组中的最大值和最小值
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    // 声明并初始化一个包含 8 个整数的测试数组
    int numbers[8] = {34, 17, 89, 5, 72, 41, 60, 23};
    int max, min;  // 分别用来存储最大值和最小值

    // ----- 算法步骤 1：假设第一个元素既是最大值又是最小值 -----
    // 这是整个算法的起点：给 max 和 min 一个初始的"基准值"
    max = numbers[0];  // 将第 1 个元素（下标 0）当作当前最大值
    min = numbers[0];  // 将第 1 个元素（下标 0）当作当前最小值

    // ----- 算法步骤 2：从第二个元素开始，逐个比较并更新 -----
    // 循环从 i = 1 开始（第二个元素），因为 i = 0 已经用作基准了
    for (int i = 1; i < 8; i++)
    {
        // 如果当前元素比已知最大值还大，就更新最大值
        if (numbers[i] > max)
        {
            max = numbers[i];  // 发现更大的值，替换 max
        }

        // 如果当前元素比已知最小值还小，就更新最小值
        if (numbers[i] < min)
        {
            min = numbers[i];  // 发现更小的值，替换 min
        }
    }

    // ----- 输出数组内容和查找结果 -----
    printf("数组内容：");
    for (int i = 0; i < 8; i++)
    {
        printf("%d ", numbers[i]);
    }
    printf("\n");

    printf("最大值：%d\n", max);  // 预期输出：89
    printf("最小值：%d\n", min);  // 预期输出：5

    return 0;
}

/*
 * ============================================================
 * 预期输出：
 * ------------------------------------------------------------
 * 数组内容：34 17 89 5 72 41 60 23
 * 最大值：89
 * 最小值：5
 * ============================================================
 *
 * 算法执行过程（逐步跟踪）：
 * i=0: 初始化 max=34, min=34
 * i=1: 17 > 34? 否。 17 < 34? 是 → min=17
 * i=2: 89 > 34? 是 → max=89。 89 < 17? 否
 * i=3: 5 > 89? 否。  5 < 17? 是 → min=5
 * i=4: 72 > 89? 否。 72 < 5? 否
 * i=5: 41 > 89? 否。 41 < 5? 否
 * i=6: 60 > 89? 否。 60 < 5? 否
 * i=7: 23 > 89? 否。 23 < 5? 否
 * 最终：max=89, min=5
 *
 * 要点总结：
 * 1. 算法核心：先假设第一个元素为最大/最小值，再逐一比较更新
 * 2. 循环从 i = 1 开始，因为 i = 0 已用作初始基准
 * 3. 两个 if 语句是独立的（不是 if-else），因为一个元素可能同时
 *    不是最大值也不是最小值，也可能恰好既更新 max 又更新 min
 * 4. 这个算法的时间复杂度为 O(n)，效率很高
 */
