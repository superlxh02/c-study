/**
 * =====================================================
 * 文件名: 08_2d_array_traversal.c
 * 所属章节: 第三章 数组
 * 对应小节: 三、二维数组 → 1.5 用嵌套循环遍历二维数组
 * 演示内容: 使用双层 for 循环（外层行、内层列）遍历二维数组
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    // 5 行 3 列的二维数组：5 个学生，每人 3 门课成绩
    int scores[5][3] = {
        {85, 92, 78},  // 学生 1
        {90, 88, 95},  // 学生 2
        {76, 82, 89},  // 学生 3
        {93, 87, 91},  // 学生 4
        {80, 85, 84}   // 学生 5
    };

    // ----- 嵌套循环遍历二维数组 -----
    // 外层循环：控制行（学生），i 从 0 到 4
    // 内层循环：控制列（科目），j 从 0 到 2
    // 执行顺序：外层 i=0 时，内层 j 从 0 跑到 2，然后外层 i 变 1...

    // 打印表头
    printf("学生成绩表：\n");
    // \t 是制表符（Tab），用来对齐列
    printf("学号\t语文\t数学\t英语\n");

    for (int i = 0; i < 5; i++)        // 外层循环：遍历每一行（每个学生）
    {
        printf("%d\t", i + 1);         // 输出学号（人类编号 = i + 1）

        for (int j = 0; j < 3; j++)    // 内层循环：遍历当前行的每一列（每门课）
        {
            printf("%d\t", scores[i][j]);  // 输出该学生该门课的成绩
        }

        printf("\n");  // 每输出完一个学生的所有成绩后换行
    }

    return 0;
}

/*
 * ============================================================
 * 预期输出：
 * ------------------------------------------------------------
 * 学生成绩表：
 * 学号    语文    数学    英语
 * 1       85      92      78
 * 2       90      88      95
 * 3       76      82      89
 * 4       93      87      91
 * 5       80      85      84
 * ============================================================
 *
 * 嵌套循环执行过程（逐元素访问）：
 *
 * 外层 i=0 | 内层 j=0: scores[0][0]=85 | j=1: scores[0][1]=92 | j=2: scores[0][2]=78 → 换行
 * 外层 i=1 | 内层 j=0: scores[1][0]=90 | j=1: scores[1][1]=88 | j=2: scores[1][2]=95 → 换行
 * 外层 i=2 | 内层 j=0: scores[2][0]=76 | j=1: scores[2][1]=82 | j=2: scores[2][2]=89 → 换行
 * 外层 i=3 | 内层 j=0: scores[3][0]=93 | j=1: scores[3][1]=87 | j=2: scores[3][2]=91 → 换行
 * 外层 i=4 | 内层 j=0: scores[4][0]=80 | j=1: scores[4][1]=85 | j=2: scores[4][2]=84 → 换行
 *
 * 要点总结：
 * 1. 双层 for 循环是处理二维数组的标准模式
 * 2. 外层循环控制行索引 i，内层循环控制列索引 j
 * 3. 程序从第 0 行第 0 列开始，从左到右一行一行地访问
 * 4. \t 制表符帮助对齐输出，但不同终端制表位宽度可能不同
 */
