/**
 * =====================================================
 * 文件名: 03_sum_average.c
 * 所属章节: 第三章 数组
 * 对应小节: 二、数组操作 → 1.2 用循环计算数组的总和与平均值
 * 演示内容: 通过累加循环求数组元素总和，并计算浮点平均值
 * =====================================================
 */

#include <stdio.h>

int main(void)
{
    // 声明并初始化成绩数组
    int scores[5] = {85, 92, 78, 90, 88};

    // sum 用于累加总分，初始值必须为 0（累加器的起点）
    int sum = 0;

    // average 声明为 float，因为平均值往往不是整数
    float average;

    // ----- 遍历数组并累加求总和 -----
    // 每轮循环将当前元素加到 sum 上
    for (int i = 0; i < 5; i++)
    {
        sum = sum + scores[i];  // sum += scores[i]; 效果相同
        // 执行过程：sum = 0 → 85 → 177 → 255 → 345 → 433
    }

    // ----- 计算平均值 -----
    // (float)sum 将 sum 临时转换为 float 类型
    // 如果不转换，整数除以整数会得到整数（小数部分被丢弃）
    // 例如：433 / 5 = 86（整数除法），(float)433 / 5 = 86.60（正确）
    average = (float)sum / 5;  // 强制类型转换，确保浮点除法

    // 输出结果
    // %d 输出整数总分
    // %.2f 输出保留 2 位小数的浮点平均值
    printf("总分：%d\n", sum);         // 预期输出：433
    printf("平均分：%.2f\n", average); // 预期输出：86.60

    return 0;
}

/*
 * ============================================================
 * 预期输出：
 * ------------------------------------------------------------
 * 总分：433
 * 平均分：86.60
 * ============================================================
 *
 * 累加过程详情：
 * i=0: sum = 0 + 85 = 85
 * i=1: sum = 85 + 92 = 177
 * i=2: sum = 177 + 78 = 255
 * i=3: sum = 255 + 90 = 345
 * i=4: sum = 345 + 88 = 433
 *
 * 要点总结：
 * 1. 累加器变量 sum 必须初始化为 0
 * 2. 循环体内逐元素累加：sum = sum + 当前元素
 * 3. 计算平均值时用 (float) 强转，避免整数除法截断
 * 4. 输出浮点数用 %.2f 控制保留 2 位小数
 */
